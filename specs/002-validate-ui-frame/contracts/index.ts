/**
 * Public barrel for UI Frame shell region contracts.
 *
 * Import from this file to access all four region interfaces:
 *
 * ```typescript
 * import {
 *   ICentralRegionTab,
 *   ISidebarEntry,
 *   IToolbarAction,
 *   IBottomPanelEntry,
 * } from '@shell/contracts'; // path alias, or relative import
 * ```
 */
export type { ICentralRegionTab } from './ICentralRegionTab';
export type { ISidebarEntry } from './ISidebarEntry';
export type { IToolbarAction } from './IToolbarAction';
export type { IBottomPanelEntry } from './IBottomPanelEntry';
